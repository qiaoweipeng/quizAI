# 智能刷题系统

智能刷题系统是一款通用的在线学习与模拟考试平台，支持多种题型和考试模式，帮助用户高效备考，提升学习效果。

## 系统定位

- **目标用户**: 需要刷题备考的各类学习者（资格考试、认证考试、学业考试等）
- **核心价值**: 提供真实的模拟考试环境，支持多种题型，帮助考生熟悉考试形式和题型
- **数据来源**: 基于JSON文件管理题库，支持自定义导入各类题目数据

## 系统功能

### 1. 考试功能

系统提供两大核心模式，满足不同学习需求：

**考试模式**

| 模式 | 说明 | 题目构成 | 评分标准 |
|------|------|----------|----------|
| **固定考卷** | 题目固定不变，支持多套试卷选择，适合模拟真实考试环境 | 100道单选 + 40道多选 + 60道判断 | 每题0.5分，满分100分，60分合格 |
| **随机考卷** | 题目从总题库随机抽取，每次考试内容不同 | 同上 | 同上 |

**练习模式**

| 模式 | 说明 | 题目构成 | 评分标准 |
|------|------|----------|----------|
| **专项练习** | 针对特定题型进行强化训练 | 自定义题型（单选/多选/判断）和数量（1~全部） | 无分数，即时显示解析 |

> **考试时间**：固定卷和随机卷限时90分钟，专项练习不限时  
> **多试卷支持**：系统支持多份独立的JSON试卷文件，便于管理和维护

### 2. 界面设计

- **设计风格**：简约清爽，专注答题体验，基于 Ant Design 设计系统
- **设备适配**：目前仅适配PC端，建议使用桌面浏览器访问
- **答题界面**：提供两种视图模式，可自由切换
  - **单题模式**：每次展示一道题，支持手动切换或答后自动跳转
  - **全览模式**：展示全部题目，通过滚动条浏览

### 3. 辅助功能

- **即时反馈**：练习模式可即时显示对错及解析
- **错题回顾**：考试结束后自动汇总错题，附带详细解析
- **一键预答**：考试模式支持快速预答（单选默认选A，多选全选，判断选对），方便测试流程
- **自动切题**：支持答题后自动跳转到下一题
- **侧边栏导航**：快速跳转到任意题目
- **时间警告**：考试剩余10分钟时自动提醒
- **题目ID显示**：答题时显示题目ID，便于排查重复题目

## 项目结构

```
fire-exam-react/
├── public/                 # 静态资源
│   └── json/              # 题目数据文件
│       ├── paper-index.json   # 试卷索引文件（记录所有试卷）
│       ├── paper-1.json       # 试卷文件1
│       ├── paper-2.json       # 试卷文件2
│       └── README.md          # 数据格式说明
├── src/                   # 源代码
│   ├── components/        # 组件目录
│   │   ├── exam/              # 考试相关组件
│   │   │   ├── ExamPage.jsx           # 考试主页面
│   │   │   ├── ExamOverview.jsx       # 全览模式组件
│   │   │   ├── ExamResultModal.jsx    # 考试结果弹窗
│   │   │   ├── ExamTimer.jsx          # 倒计时组件
│   │   │   ├── QuestionCard.jsx       # 题目卡片组件
│   │   │   ├── QuestionOption.jsx     # 选项组件
│   │   │   └── QuestionSidebar.jsx    # 题目导航侧边栏
│   │   ├── home/              # 首页相关组件
│   │   │   ├── HomePage.jsx           # 首页组件
│   │   │   ├── ExamModeSelector.jsx   # 考试模式选择器
│   │   │   └── PracticeModeSelector.jsx # 练习模式选择器
│   │   ├── practice/          # 练习相关组件
│   │   │   └── PracticePage.jsx       # 练习页面
│   │   └── result/            # 结果相关组件
│   │       ├── ResultPage.jsx         # 结果页面
│   │       ├── ResultDisplay.jsx      # 结果展示组件
│   │       └── WrongAnswerModal.jsx   # 错题预览弹窗
│   ├── hooks/             # 自定义Hooks
│   │   └── useExamLogic.js      # 考试核心逻辑Hook
│   ├── store/             # 状态管理
│   │   └── examStore.ts         # Zustand状态管理
│   ├── types/             # TypeScript类型定义
│   │   └── index.ts             # 全局类型定义
│   ├── utils/             # 工具函数
│   │   └── examUtils.js          # 工具函数（加载文件、计分、格式化等）
│   ├── App.css            # 主样式
│   ├── App.jsx            # 主应用组件
│   ├── index.css          # 全局样式
│   └── main.jsx           # 入口文件
├── .gitignore             # Git忽略配置
├── .npmrc                # pnpm配置
├── index.html            # HTML模板
├── package.json          # 项目配置
├── vite.config.js        # Vite配置
└── README.md             # 项目说明
```

## 技术栈

- **前端框架**: React 19
- **构建工具**: Vite 5
- **包管理器**: pnpm
- **UI组件库**: Ant Design 6
- **状态管理**: Zustand
- **类型系统**: TypeScript
- **样式**: CSS3 + Ant Design
- **数据存储**: JSON 文件（支持多文件管理）

## 快速开始

### 安装依赖

```bash
pnpm install
```

### 开发模式

```bash
pnpm dev
```

启动后访问 http://localhost:5173/

### 构建生产版本

```bash
pnpm build
```

构建产物会输出到 `dist/` 目录。

### 预览生产构建

```bash
pnpm preview
```

## 多试卷管理

系统支持多份独立的JSON试卷文件，采用**索引+试卷**的管理方式：

### 1. 创建试卷索引 (`paper-index.json`)

```json
{
  "papers": [
    {
      "id": "paper-1",
      "name": "历年真题练习（卷A）",
      "file": "paper-1.json"
    },
    {
      "id": "paper-2",
      "name": "模拟试卷（卷B）",
      "file": "paper-2.json"
    }
  ]
}
```

### 2. 创建试卷文件 (`paper-xxx.json`)

每个试卷文件只包含题目数组：

```json
{
  "questions": [
    {
      "id": "dx-1",
      "type": "single",
      "question": "题目内容",
      "options": ["A. 选项1", "B. 选项2", "C. 选项3", "D. 选项4"],
      "answer": ["A"],
      "parse": "解析内容"
    }
  ]
}
```

### 3. 添加新试卷步骤

1. 在 `paper-index.json` 中添加新试卷的元数据（id、name、file）
2. 创建对应的试卷文件（如 `paper-3.json`）
3. 刷新页面即可自动加载新试卷，无需修改代码

## 题目数据格式

题目数据存储在 `public/json/` 文件夹中，采用**多文件模式**：
- `paper-index.json` - 试卷索引文件
- `paper-xxx.json` - 各试卷文件

### 字段说明

| 字段 | 类型 | 说明 |
|------|------|------|
| `type` | string | 题目类型：`single`(单选)、`multiple`(多选)、`judge`(判断) |
| `question` | string | 题目内容 |
| `options` | array | 选项数组 |
| `answer` | array | 正确答案数组，如 `["A"]` 或 `["A","B"]` |
| `parse` | string | 解析内容 |

## 添加新题目

1. 在 `paper-index.json` 中添加试卷条目（包含 id、name、file）
2. 创建对应的试卷文件（如 `paper-3.json`）
3. 无需修改代码，刷新页面即可加载

## 注意事项

- 题目文件必须放在 `public/json/` 文件夹中
- JSON 文件名建议使用英文，避免编码问题
- 确保题目 ID 唯一，避免重复
- 目前系统仅适配 PC 端，建议使用桌面浏览器访问
- 试卷文件名需与 `paper-index.json` 中的 `file` 字段对应

### 依赖更新规范

> **重要**：如果项目依赖发生变化（新增/删除/升级依赖包），或者 `package.json`、`pnpm-lock.yaml` 文件有任何修改，**必须先执行 `pnpm install` 更新依赖**，然后再进行开发或构建。

```bash
# 更新依赖（必须执行）
pnpm install

# 开发模式
pnpm dev

# 构建生产版本
pnpm build
```

**为什么必须执行 `pnpm install`？**

1. **锁定依赖版本**：`pnpm-lock.yaml` 文件记录了每个依赖的确切版本，确保开发环境和部署环境使用相同的依赖版本
2. **避免 Netlify 构建失败**：如果推送的代码包含新的依赖但没有运行 `pnpm install`，Netlify 在构建时可能会找不到新依赖，导致构建失败
3. **保持一致性**：确保团队成员使用相同的依赖版本，避免"在我电脑上能跑"的问题

**何时需要执行 `pnpm install`？**

- 修改了 `package.json` 文件中的 `dependencies` 或 `devDependencies`
- 从远程仓库拉取了包含依赖变更的代码
- 删除了 `node_modules` 文件夹
- 首次克隆项目后

## 开发说明

- 使用 pnpm 作为包管理器
- 题目数据通过 fetch API 从 public 文件夹加载
- 支持热更新，修改代码后自动刷新
- 组件采用模块化设计，便于维护和扩展
- 使用 Zustand 进行全局状态管理
- 使用 TypeScript 进行类型安全开发
- 使用 Ant Design 组件库构建 UI

## 部署说明

### GitHub + Netlify 部署

> **部署前重要提醒**：确保在推送代码到 GitHub 之前，已经在本地执行过 `pnpm install`，并且 `pnpm-lock.yaml` 文件已经更新。否则 Netlify 在构建时可能会因为依赖版本不匹配而失败。

1. 在本地执行 `pnpm install` 更新依赖
2. 将项目推送到 GitHub 仓库
3. 登录 Netlify，点击「New site from Git」
4. 选择你的 GitHub 仓库
5. 配置构建命令：`pnpm build`
6. 配置发布目录：`dist`
7. 点击「Deploy site」完成部署

### 其他部署方式

```bash
# 构建生产版本
pnpm build

# 使用静态服务器部署
cd dist
npx serve
```

## 待办功能清单 (TODO)

### 用户系统

- [ ] 用户管理功能
  - [ ] 手机号+验证码登录
  - [ ] 会员权限管理（开通会员后可使用系统答题）

### 支付系统

- [ ] 用户支付功能
  - [ ] 支付宝支付接入
  - [ ] 微信支付接入

### 数据存储升级

- [ ] 数据库迁移
  - [ ] 考试数据迁移至 MySQL
  - [ ] 用户数据迁移至 MySQL
  - [ ] 系统安全性和健壮性增强

### 专项练习优化

- [ ] 章节选择功能

### AI 智能功能

- [ ] AI 智能分析
  - [ ] 分析用户答题结果
  - [ ] 给出个性化学习建议
- [ ] AI 自动出题
  - [ ] 根据用户答题结果智能生成练习题

## 版本历史

### v1.4 (当前版本)

**UI交互优化与功能增强**
 - ✅ 选项点击抖动修复：设置固定宽度，解决字体加粗导致的布局抖动
 - ✅ 全览模式滚动优化：点击题目列表自动滚动到可视区域，并添加淡紫色突出动画效果
 - ✅ 当前题目样式优化：调整字体颜色和边框流光效果，使其更鲜艳
 - ✅ 练习模式优化：移除选项中的A、B、C、D标识，退出确认使用Ant Design Modal
 - ✅ 全屏功能：添加全屏/退出全屏按钮，支持浏览器全屏API
 - ✅ 交卷后空白页修复：保留考试状态，避免页面空白
 - ✅ 单题模式题目索引显示修复
 - ✅ 题目ID交互优化：双击题目直接复制ID，使用Alert提示已复制
 - ✅ 错题本功能：新增错题本抽屉，支持右键添加错题、表格展示、一键清空
 - ✅ 错题本徽标：使用Ant Design Badge显示错题数量
 - ✅ 多层抽屉效果：错题本表格点击行弹出题目详情抽屉，UI与预览模式一致
 - ✅ 考试结果模态框统一：使用统一的图标和标题，关闭逻辑优化（仅错题数为0时返回首页）
 - ✅ 图标替换：一键预选使用ThunderboltOutlined，考试模式使用FileDoneOutlined/FileSyncOutlined/FileSearchOutlined
 - ✅ 预览模式优化：隐藏自动切题按钮
 - ✅ 选项hover效果优化：仅在鼠标悬停答案文本时触发

### v1.3

**UI 升级**

- ✅ 引入 Ant Design UI 组件库，统一界面风格
- ✅ 按钮样式优化：进入练习、开始考试、交卷等按钮使用 Ant Design 渐变效果
- ✅ 一键预选通知改为使用 Ant Design Notification 组件
- ✅ 模态框按钮使用 Ant Design 默认样式

**功能优化**

- ✅ 一键预选通知逻辑修复：仅在首次生成试卷时显示，刷新不重复显示
- ✅ 通知关闭功能修复：点击确认按钮可正常关闭通知
- ✅ 删除无用组件 ResultPage.jsx
- ✅ 代码性能优化：使用 useCallback 优化函数引用

**技术实现**

- 组件样式：使用 Ant Design 组件库
- 构建优化：移除未使用的组件导入

### v1.1

**新增功能**

- ✅ 多试卷管理：支持多份独立的JSON试卷文件
- ✅ 组件模块化：代码拆分为独立组件，便于维护
- ✅ 工具函数抽取：公共逻辑统一管理

**核心功能**

- ✅ 两种模式：
  - ✅ 考试模式：固定题目/随机题目，200道题，90分钟限时，60分及格
  - ✅ 练习模式：自定义题型（单选/多选/判断）、题目数量（1~全部）

- ✅ 即时反馈：答题后立即显示对错及解析
- ✅ 成绩统计：详细的成绩分析和错题回顾
- ✅ 错题回顾：考试结束后自动汇总错题，附带详细解析
- ✅ 一键预答：考试模式支持快速预答功能
- ✅ 自动切题：支持答题后自动跳转到下一题
- ✅ 侧边栏导航：快速跳转到任意题目

**界面设计**

- ✅ 简约清爽的UI风格
- ✅ 考试模式占2/3，练习模式占1/3的布局设计
- ✅ 单题模式和全卷模式两种答题视图可切换
- ✅ 浏览器前进/后退按钮支持（history API）

**技术实现**

- 数据存储：JSON 文件存储题目数据（支持多文件管理）
- 部署平台：GitHub + Netlify
- 前端框架：React 18 + Vite 5
- 包管理器：pnpm

**已知限制**

- 题目管理需手动修改 JSON 文件
- 专项练习暂不支持考试大纲选择功能
- 暂不支持用户系统和数据持久化
- 仅适配 PC 端

### v1.0

- 初始版本
- 支持固定卷和随机卷考试模式
- 支持练习模式
- 单文件数据存储